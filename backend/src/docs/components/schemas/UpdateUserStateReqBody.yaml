type: object
required:
  - verify
properties:
  verify:
    type: integer
    description: The new verification status for the user. (0 - Unverified, 1 - Verified, 2 - Banned)
    enum: [0, 1, 2]
    example: 1